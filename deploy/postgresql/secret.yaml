# WARNING: Placeholder credentials. Replace POSTGRES_PASSWORD with a
# strong, generated value (e.g. `openssl rand -hex 16`) BEFORE running
# `oc apply`. For production, use OpenShift Secrets management or
# HashiCorp Vault.
apiVersion: v1
kind: Secret
metadata:
  name: memoryhub-pg-credentials
  labels:
    app.kubernetes.io/name: memoryhub-pg
    app.kubernetes.io/part-of: memoryhub
    app.kubernetes.io/component: database
type: Opaque
stringData:
  POSTGRES_USER: memoryhub
  POSTGRES_PASSWORD: REPLACE-ME-GENERATE-STRONG-PASSWORD
  POSTGRES_DB: memoryhub
