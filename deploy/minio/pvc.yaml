apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: memoryhub-minio-data
  labels:
    app.kubernetes.io/name: memoryhub-minio
    app.kubernetes.io/part-of: memoryhub
    app.kubernetes.io/component: storage
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
