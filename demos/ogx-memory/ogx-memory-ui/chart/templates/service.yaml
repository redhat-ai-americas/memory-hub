apiVersion: v1
kind: Service
metadata:
  name: {{ include "ui-template.fullname" . }}
  labels:
    {{- include "ui-template.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ui-template.selectorLabels" . | nindent 4 }}
