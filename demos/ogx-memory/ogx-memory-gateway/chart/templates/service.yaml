apiVersion: v1
kind: Service
metadata:
  name: {{ include "gateway-template.fullname" . }}
  labels:
    {{- include "gateway-template.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gateway-template.selectorLabels" . | nindent 4 }}
