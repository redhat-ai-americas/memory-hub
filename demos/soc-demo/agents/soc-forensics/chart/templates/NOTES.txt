Agent "{{ include "soc-forensics.fullname" . }}" deployed to namespace {{ .Release.Namespace }}.

Check pod status:

  oc get pods -l app.kubernetes.io/instance={{ .Release.Name }} -n {{ .Release.Namespace }}

View agent logs:

  oc logs -l app.kubernetes.io/instance={{ .Release.Name }} -n {{ .Release.Namespace }} -f

{{- if .Values.route.enabled }}

Route:

  https://{{ .Values.route.host | default (printf "%s-%s.apps.cluster.example.com" (include "soc-forensics.fullname" .) .Release.Namespace) }}
{{- end }}

Override agent.yaml config by setting values under the `config` key:

  helm upgrade {{ .Release.Name }} chart/ \
    --set config.MODEL_ENDPOINT=https://vllm.apps.cluster.example.com/v1 \
    --set config.LOG_LEVEL=WARNING \
    -n {{ .Release.Namespace }}
