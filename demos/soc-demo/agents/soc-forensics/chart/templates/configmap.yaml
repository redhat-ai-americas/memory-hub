apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "soc-forensics.fullname" . }}-config
  labels:
    {{- include "soc-forensics.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
